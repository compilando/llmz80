name: CI

on:
    push:
        branches: [main, develop]
    pull_request:
        branches: [main, develop]

jobs:
    test:
        runs-on: ${{ matrix.os }}
        strategy:
            fail-fast: false
            matrix:
                os: [ubuntu-latest, macos-latest, windows-latest]
                python-version: ["3.10", "3.11", "3.12"]

        steps:
            - uses: actions/checkout@v4

            - name: Set up Python ${{ matrix.python-version }}
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ matrix.python-version }}

            - name: Cache pip packages
              uses: actions/cache@v4
              with:
                  path: ~/.cache/pip
                  key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
                  restore-keys: |
                      ${{ runner.os }}-pip-

            - name: Install dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install -r requirements.txt
                  pip install -r requirements-dev.txt

            - name: Lint with flake8
              run: |
                  # Syntax errors and undefined names fail the build.
                  flake8 llmz80 tests scripts --count --select=E9,F63,F7,F82 --show-source --statistics
                  # Everything else is reported and does not.
                  flake8 llmz80 tests scripts --count --exit-zero --statistics

            - name: Check code formatting with black
              run: |
                  black --check llmz80 tests scripts

            - name: Check import sorting with isort
              run: |
                  isort --check-only llmz80 tests scripts

            # No continue-on-error: the tree is clean under this as of the
            # legacy-generator removal, and a type error that lands after it
            # should be seen when it lands rather than found months later.
            - name: Type check with mypy
              run: |
                  mypy llmz80 --ignore-missing-imports

            - name: Run tests with pytest
              run: |
                  pytest tests/ -v --cov=llmz80 --cov-report=xml --cov-report=term

            - name: Upload coverage to Codecov
              uses: codecov/codecov-action@v4
              with:
                  file: ./coverage.xml
                  flags: unittests
                  name: codecov-umbrella
                  fail_ci_if_error: false

    security:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: "3.10"

            - name: Install dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install bandit[toml]

            - name: Security check with bandit
              run: |
                  bandit -r llmz80 -c pyproject.toml

    docs:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: "3.10"

            - name: Install dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install sphinx sphinx-rtd-theme myst-parser

            - name: Check documentation builds
              run: |
                  # This will be enabled when we add Sphinx docs
                  echo "Documentation check placeholder"
              continue-on-error: true
